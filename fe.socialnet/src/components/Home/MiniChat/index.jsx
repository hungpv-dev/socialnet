import classNames from "classnames/bind";
import styles from './MiniChat.module.scss';

const cx = classNames.bind(styles);

function MiniChat(idChat){
    return <div className={cx('mini-chat')}>
        
    </div>;
}

export default MiniChat;
